{
  "name": "sistema-operadora-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix ./servico-gestao run build && npm --prefix ./servico-faturamento run build && npm --prefix ./servico-planos-ativos run build && npm --prefix ./api-gateway run build",
    "build:gestao": "npm --prefix ./servico-gestao run build",
    "build:faturamento": "npm --prefix ./servico-faturamento run build",
    "build:planos-ativos": "npm --prefix ./servico-planos-ativos run build",
    "build:gateway": "npm --prefix ./api-gateway run build",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "amqplib": "^0.10.5",
    "axios": "^1.8.1",
    "express": "^4.21.2",
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/jest": "^29.5.0",
    "@types/node": "^22.13.11",
    "jest": "^29.6.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.8.2"
  }
}
